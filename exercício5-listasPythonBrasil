# 5) Faça um Programa que leia 20 números inteiros e armazene-os num vetor. Armazene os números pares no vetor PAR e os números IMPARES no vetor impar. Imprima os três vetores.

numeros = []
pares = []
impares = []
numero = 0
'''for c in range(0,6):                                      # resolução online
    numeros.append(int(input(f'Digite o {c + 1}º valor: ')))
    numero = numeros[c]
    if numero % 2 == 0:
        pares.append(numero)
    else:
        impares.append(numero)'''

for c in range(0,6):                                       # minha resolução
    numero = int(input(f'Digite o {c + 1}º valor: '))
    numeros.append(numero)
    if numero % 2 == 0:
        pares.append(numero)
    else:
        impares.append(numero)
numeros.sort()
pares.sort()
impares.sort()
print(numeros)
print(pares)
print(impares)